[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "aorta4llm"
version = "0.1.0"
description = "Organizational reasoning for LLM agent systems"
requires-python = ">=3.10"
dependencies = [
    "pyswip>=0.3.1",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
]
dashboard = [
    "flask>=3.0",
]
orchestrator = [
    # Requires Claude Code CLI: npm install -g @anthropic-ai/claude-code
]

[tool.hatch.build.targets.wheel]
packages = ["governance", "integration", "dashboard", "orchestrator"]

[tool.pytest.ini_options]
testpaths = ["governance/tests"]
